 -- prática 1 local hust
 CREATE DATABASE Yan_Duarte;
 USE Yan_Duarte;
CREATE TABLE CLIENTE
 ( CODIGO INT AUTO_INCREMENT,  
 NOME VARCHAR(50) NOT NULL,
 EMAIL TEXT NOT NULL,
 DATA DATETIME NOT NULL,
 PRIMARY KEY(CODIGO) );

 INSERT INTO CLIENTE (NOME,EMAIL, DATA) VALUES ('CMN', 
'CMN@CMN.BR', NOW() );
 INSERT INTO CLIENTE (NOME,EMAIL, DATA) VALUES ('ANA', 
'ANA@ANA.BR', NOW() );
 INSERT INTO CLIENTE (NOME,EMAIL, DATA) VALUES ('BETO', 
'BETO@ANA.BR', NOW() );
  SELECT* FROM CLIENTE;
  
  SHOW TABLE STATUS WHERE NAME = 'CLIENTE';
  ALTER TABLE CLIENTE ENGINE = MyISAM;
  -- MyISAM
  
  describe cliente;

  select * from cliente;

-- prática 2
CREATE TABLE PEDIDO ( 
NUMERO INT AUTO_INCREMENT PRIMARY KEY, 
CLIENTE INT,
VALOR DECIMAL(10,2), 
FOREIGN KEY(CLIENTE) REFERENCES CLIENTE (CODIGO))  
ENGINE=InnoDB;

 desc pedido;

 SHOW TABLE STATUS WHERE NAME = 'PEDIDO';
 
 INSERT INTO PEDIDO VALUES(2025,1,1.99), 
(2026,2,150.90), (2027,3,9.99);

SELECT* FROM CLIENTE, PEDIDO
WHERE CLIENTE.CODIGO = PEDIDO.CLIENTE;

-- prática 3
 LOCK TABLES CLIENTE WRITE;
UNLOCK TABLES; 